# Data Folder

This folder holds the real CSV files used by the project.

- `world_bank_gdp.csv`
- `comtrade_trade.csv`
- `country_coordinates.csv` (optional but recommended)

The current code is real-data only. It does not fall back to a demo graph.
